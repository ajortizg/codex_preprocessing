# @package _global_

defaults:
  - override /data: raw_codex
  - override /pipeline: default
  - override /pipeline/tma_dearray: default

task_name: "preprocess_ccc"

data:
  # root_dir: "outputs/preprocess_ccc/2025-10-20/11-19-48/tma_dearray"
  # root_dir: "/storage/groups/interpret_tme/codex_CCC/raw/MY_TMA_CCC_E_CIM"
  root_dir: "outputs/preprocess_ccc/2025-10-30/12-03-04/illumination_correction"

pipeline:
  remove_intermediate: false

  deconvolution:
    algorithm:
      scale_factor: 0
      n_iter: 25
      use_gpu: true
    devices: [0, 0]

  edof:
    devices: [0, 0]

  illumination_correction:
    n_jobs: 4

  tma_dearray:
    n_jobs: 4
    ref_cycle: 2
    ref_channel: 1

  data_export:
    algorithm:
      remove_markers: ["Empty", "Blank"]
      rename_dict:
        "SLC7A5/LAT1": "SLC7A5_LAT1"
